<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cssl.dao.ProductPhotoUpDao">
    <select id="ppuList" resultType="ProductPhotoUp">
    select * from productphotoup pd where pd.productcode=#{pid}
    </select>
</mapper>